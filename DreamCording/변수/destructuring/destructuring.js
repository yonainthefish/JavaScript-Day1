const object = {a:1 , b:2};

const a = object.a;
const b = object.b;

console.log(a)
console.log(b)

// 비구조 할당
// 객체 안에 있는 값을 {} 중괄호 로 사용 가는

const arr = [1,2];
const [one,two] = arr

console.log(one);
console.log(two);